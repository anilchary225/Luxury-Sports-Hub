{
  "version": 3,
  "installCommand": "pnpm install --frozen-lockfile",
  "builds": [
    {
      "src": "artifacts/msk-academy/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/public"
      }
    }
  ]
}
